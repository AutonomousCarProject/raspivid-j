apply plugin:'java'

sourceCompatibility = 1.8

wrapper {
    gradleVersion = '4.9'
}

subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    dependencies {
        compile 'org.jetbrains:annotations:15.0'
    }
}